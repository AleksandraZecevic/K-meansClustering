package util;

public enum LogLevel {
    // enumeration - special data type used to define a set of named constants
    // allows us to represent a fixed group of related values in type-safe way
    Warn,
    Error,
    Info,
    Debug,
    Status,
    Success,
}
